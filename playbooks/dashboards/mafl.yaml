- name: Mafl
  hosts: dashboards
  become: yes
  tasks:
    # ####################################
    # Prepare files
    # ####################################
    - name: Template mafl.yml config
      template:
        dest: /configs/mafl/config.yml
        src: ../../backups-and-confs/mafl/config.yml.j2
